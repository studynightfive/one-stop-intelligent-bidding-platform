# 一站式智能招投标平台前端 Demo 优化实施汇总 V1.1

更新时间：2026-08-06
参考建议：`一站式智能招投标平台前端Demo优化改进建议_V1.0.md`

## 一、实施原则

本轮只处理能提升前端 Demo 功能完整性、操作闭环和演示可信度的建议，不以补充业务数据为目标。依赖真实后端、安全基础设施或外部服务的能力不会在前端伪装成已完成。

## 二、已采用并完成的优化

### 1. 产品命名与内外部边界

- “做标书 / 评标书”统一调整为“投标中心 / 评标中心”。
- 工作台名称统一为“投标工作台 / 评标工作台”。
- 供应商入口明确标注为“供应商门户（外部）”。
- 顶部增加“演示环境”标识，侧栏继续提示操作保存在当前浏览器。
- 通知来源中的“做标”统一改为“投标”。

### 2. 投标工作台效率与信息表达

- 统计卡片可点击下钻到对应任务状态。
- 新增项目名称、招标编号、招标方关键词搜索。
- 新增负责人筛选，以及“我的项目 / 临近截止 / 风险项目”快捷筛选。
- 新增筛选重置与当前结果 CSV 导出。
- 截止日期增加“剩余 N 天 / 今天截止 / 已逾期 N 天”等相对时间。
- 将进度明确为“综合进度”，并提示其由流程阶段和材料完整度综合计算。
- 缺失材料数量可点击查看具体材料和要求。
- 行操作收纳到“更多”，支持复制项目、导出项目概览、直接发起评标任务。
- 空结果增加清除筛选入口。

### 3. 投标与评标任务打通

- 投标任务详情新增“发起评标任务 / 查看关联评标”入口。
- 创建评标任务时可从已有投标项目导入。
- 自动带入项目名称、招标编号、采购单位、截止时间和材料清单。
- 通过投标项目编号识别关联评标任务，形成投标到评标的前端闭环。

### 4. 评标任务创建完整性

- 新增手动保存草稿、自动保存状态和恢复草稿。
- 新增发布前预览，集中展示项目、材料、权重、供应商和评审人配置。
- 新增取消创建确认，避免误退出。
- 新增供应商提交截止时间必须晚于当前时间的校验。
- 新增评标开始时间必须晚于供应商提交截止时间、结束时间必须晚于开始时间的校验。
- 保留评分权重必须等于 100、供应商和评审人不能为空的校验。
- 发布前增加二次确认；任务编号改为时间戳形式，降低 Demo 内重复概率。
- 发布成功后自动清理已完成草稿。

### 5. 评标工作台与风险下钻

- 新增项目关键词和负责人筛选、重置筛选及结果数量提示。
- 统计卡片可下钻到材料收集、AI 初审等状态。
- “废标预警”可打开风险明细，并明确 AI 只提供复核线索、不自动执行废标。
- 截止日期增加相对时间，预算金额改为“万元”简写并保留完整金额提示。
- AI 初审统一使用紫色状态表达，人工复审名称保持一致。
- 评审进度增加计算口径提示，筛选空状态和看板空列均有反馈。

### 6. 供应商邀请与提交门户

- 供应商列表为每家供应商提供“复制专属链接”。
- 专属链接进入门户后锁定对应供应商，不能切换其他供应商视角。
- 统一入口明确限定为内部 Demo 切换视角使用。
- 专属入口明确提示：当前令牌仅为前端模拟，正式环境需服务端验证身份、权限和有效期。
- 材料提交新增本机草稿保存。
- 完成材料提交增加二次确认。
- 正式提交后可下载 Demo 提交回执。

### 7. 资质库与文档片段库

- 资质增加发证机构和文件版本字段，并在列表、预览和编辑中展示。
- 资质有效期改为按当前日期动态计算剩余或逾期天数，移除硬编码提示。
- 文档片段新增“关键词 / AI 语义推荐”两种检索视图。
- AI 推荐片段展示匹配度，并在预览中说明推荐原因和人工确认边界。
- 片段增加来源与版本元数据，新上传片段自动记录来源和 `v1.0` 版本。

### 8. 用户与权限操作

- 顶部统计统一为总用户、活跃用户、未激活/停用、部门数，避免角色和状态混用。
- 新增角色、部门、状态组合筛选与重置。
- 新增联系电话字段及格式校验、邮箱重复校验。
- 参与项目数量可点击查看汇总。
- “编辑”之外的操作统一收纳到“更多”。
- 密码操作改为发送重置邮件，不在页面生成或展示临时密码。
- 停用/启用用户增加影响说明和二次确认。
- 新增最近活动记录查看入口。

### 9. AI 模型配置

- 将“模型服务商与凭据”和“业务场景模型路由”拆为两个独立配置层。
- 阿里云百炼、DeepSeek、智谱 AI 分别配置 Base URL、脱敏密钥、启用状态和连接测试。
- 按招标解析、需求提取、投标生成、内容审核、评标分析分别配置主模型和降级模型。
- 增加 Top P、最大输出 Token、请求超时等参数，并统一参数命名。
- 增加未保存状态、保存时间和底部固定保存栏。
- 明确提示正式环境密钥必须由服务端加密、鉴权并记录审计日志。

### 10. 可用性与可访问性

- 提升小号辅助文字的对比度，保留较浅的输入占位符层级。
- 增加统一键盘焦点轮廓。
- 保留并强化减少动态效果的系统偏好支持。
- 新增筛选空状态、风险详情、确认弹窗、成功/失败反馈等交互状态。

## 三、验证结果

- `npm run build`：通过（TypeScript 编译和 Vite 生产构建均成功）。
- 浏览器回归：通过。
  - 投标工作台筛选、风险材料下钻。
  - 投标项目导入评标任务、草稿保存与发布预览。
  - 评标工作台风险下钻。
  - 供应商专属链接与身份锁定。
  - 片段 AI 语义推荐、来源和版本展示。
  - 用户筛选与“更多”操作。
  - 模型服务商、场景路由和连接测试。
  - 资质版本、发证机构和动态有效期。
- 桌面端视觉检查：投标工作台、评标创建页、系统模型配置页未发现内容遮挡或主要布局异常。

## 四、暂缓项（需要后端或基础设施）

- 真实登录、RBAC 权限校验、租户隔离和接口级鉴权。
- API Key 服务端加密保存、密钥轮换和真实模型连接。
- 供应商邀请令牌签名、有效期、撤销、身份核验和防重放。
- 文件分片上传、断点续传、病毒扫描、对象存储和服务端解析。
- 不可篡改审计日志、电子签章、时间戳和证据链。
- AI 真实调用、费用统计、延迟监控、质量评估与告警。
- 邮件、短信、企业微信等真实通知通道。
- 大数据量服务端分页、批处理、异步任务和并发控制。

这些能力不能仅靠前端可靠实现，当前 Demo 已在相关页面标出边界，避免演示时被误认为已具备生产安全能力。
