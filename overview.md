# 项目交付总览

## 项目：一站式智能招投标平台

> 版本：v2.0
> 日期：2026-08-05

---

## 产品定位

覆盖企业采购招投标全流程的一站式平台，通过AI多智能体系统实现**做标书**和**评标书**双业务线：
- **做标书**（7步闭环）：上传招标文件 → AI拆解需求 → 材料清单 → 下载模板 → 批量上传 → AI审核 → 输出Word+版本管理
- **评标书**（6步闭环）：发布评标任务 → 供应商在线提交 → 材料审核（完整性检查+废标检测） → 评标评审（AI初评+人工复审） → 结果公示 → 评标关闭+操作留痕

---

## 交付物清单

### 1. PRD.md — 产品需求文档 v2.0
- 做标书+评标书双方向竞品分析（5+3个做标书竞品 + 3个评标竞品 + 3个替代方案）
- 用户画像（5个角色：管理员/项目负责人/成员/审核员/供应商）
- 做标书功能清单（10个P0：F1-F10）+ 评标书功能清单（8个P0：F11-F18）
- 做标书7步闭环 + 评标书6步闭环业务流程详细描述
- 管理后台设计（资质库 + 文档片段库 + 评标任务管理 + 自动匹配）
- 非功能需求 + 32条EARS验收标准 + 边界条件 + 6个里程碑

### 2. ARCHITECTURE.md — 架构设计文档 v2.0
- 双业务线统一架构（做标书8 Agent + 评标书5 Agent，共享模型路由层）
- 完整技术栈选型矩阵（React 18 + FastAPI + PostgreSQL 16 + LangGraph）
- 做标书8个Agent + 评标书5个Agent设计 + 协作流程图 + LangGraph图结构映射
- 供应商门户架构（Portal Token认证 + WebSocket实时通知）
- 评标操作留痕设计（Append-only审计日志 + 触发器保护）
- 文档处理：PyMuPDF+pdfplumber解析，docxtpl+python-docx生成，GLM-4.6V资质真伪识别
- 多租户架构 + 可行性验证 + 风险评估 + 成本估算（做标书~¥0.30/任务，评标~¥1.05/任务）
- 11条ADR决策记录

### 3. DESIGN.md — 前端设计文档 v2.0
- 设计方向：Swiss Minimalism + Data Density
- 对标品牌：Linear（主）+ Stripe Dashboard（副）+ 飞书项目（补充）
- 设计Token系统：配色（主色#2563EB，评标标识色#7C3AED纯色）、字体（Inter+Noto Sans SC）、图标（Lucide React锁定）
- 双模式导航设计（做标书/评标书模式切换）
- 18个页面清单与路由（10个做标书 + 4个评标书 + 4个评标Tab页面）
- 做标书6个关键页面 + 评标书4个关键页面详细布局
- 供应商门户设计规范（截止倒计时、材料上传、多轮报价、补材料通知、评标结束页面）
- AI交互设计规范 + 响应式策略 + 无障碍基线

### 4. SPEC.md — 规格契约文档 v2.0
- MVP范围锁定（18个P0功能：F1-F18）
- Out-of-Scope锁定（13项不做+原因+何时考虑）
- 技术架构锁定（含版本锚定 + 供应商Portal Token认证）
- API端点清单（17个模块，80+端点）
  - 做标书：认证/任务/招标文件/材料清单/材料上传/AI审核/文档输出/资质库/文档片段/用户权限/通知（11模块）
  - 评标书：评标任务/供应商门户/多轮报价/评分/补材料通知/操作留痕（6模块）
- 数据库表清单（32张表：18张做标书 + 14张评标书，含审计日志触发器设计）
- 页面清单（18个页面，路由+组件+API映射）
- 设计Token锁定
- 32条EARS验收标准（12条做标书 + 12条评标书 + 8条通用）
- 端到端验证步骤（做标书+评标书双流程curl命令）
- 预防性已知坑提醒（8条）

---

## 交叉验证结果

| 检查项 | 结果 |
|--------|------|
| PRD功能→架构Agent覆盖 | 18/18全覆盖（做标书8 Agent + 评标书5 Agent） |
| 图标库一致性 | PM/架构师/设计师三方一致选择Lucide React |
| 配色一致性 | 设计师#2563EB Token系统为权威版本 |
| 评标业务线API→DB表映射 | 评标6模块API均有对应DB表 |
| 供应商门户认证→架构设计 | Portal Token方案在架构和Spec中一致 |
| 审计日志设计→验收标准 | AC-22验证日志不可篡改 |
| 无阻断性不一致 | 通过 |

---

## 技术栈速览

| 层 | 技术 |
|----|------|
| 前端 | React 18 + TypeScript + Ant Design 5 + Tailwind CSS + Lucide React |
| 后端 | FastAPI + SQLAlchemy 2.0 + Pydantic 2 |
| 数据库 | PostgreSQL 16 + pgvector + Redis 7 |
| 文件存储 | MinIO（S3兼容） |
| AI（做标书） | LangGraph + Qwen-Plus/DeepSeek-V3/GLM-4（8 Agents） |
| AI（评标书） | LangGraph + Qwen-Plus/DeepSeek-V3/GLM-4.6V（5 Agents） |
| 实时通信 | WebSocket（供应商门户+进度推送） |
| 部署 | Docker（SaaS: ACK / 私有化: Compose） |

---

## Demo信息

前端Demo已构建完成，基于模拟数据展示完整交互流程：
- 路径：/Users/teacher/WorkBuddy/2026-08-04-22-23-58/demo/
- 启动：`npm run dev`（端口3210）
- 做标书模式：工作台、任务详情（7步闭环）、资质库、文档片段库、用户权限、系统设置
- 评标书模式：评标工作台、创建评标任务、评标详情（7个Tab）、供应商门户
- 双模式切换导航，全部使用Lucide图标，设计Token配色
